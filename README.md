# Anime Witcher Web — Vercel deployment branch

This branch materializes the complete Next.js source from `source.tgz` during install and then builds it normally on Vercel. Full readable source also exists in `awoadak-glitch/STREMING_WEP` branch `anime-witcher-web`.
